// ============================================================
// Version de l'application — SOURCE UNIQUE DE VÉRITÉ (affichage)
// ============================================================
// Pas de package_info_plus (plugin natif) : cohérent avec la politique
// "zéro dépendance native -> patchable Shorebird" du projet.
//
// Version d'AFFICHAGE (sans le build metadata "+N" de pubspec).
//   pubspec : version: 1.0.2+3   ->   kAppVersion = '1.0.2'
// ⚠️ À mettre à jour à chaque bump de version applicative.
// ============================================================

const String kAppVersion = '1.0.2';
